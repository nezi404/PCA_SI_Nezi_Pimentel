#include <stdio.h>

int main() {
  int cont = 0, num = 0, num1 ;
  while (cont < 5){
      cont = cont + 1;
      printf("Numeros para soma: ");
      scanf("%d", &num1);
      num = num1 + num;}
printf("a soma: %d", num);
  
}

// twm que ter so uma variavel
